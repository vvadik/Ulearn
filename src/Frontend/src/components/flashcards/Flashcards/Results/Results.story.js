import React from "react";
import Results from "./Results";

export default {
	title: "Cards/Results",
};

export const Default = () => <Results handleClick={() => {}} />;

Default.storyName = "default";
