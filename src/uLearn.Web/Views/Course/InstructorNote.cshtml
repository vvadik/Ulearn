@using uLearn
@model IntructorNoteModel

@{
	ViewBag.Title = "Заметки преподавателю";
}


@Html.Action("TableOfContents", "SlideNavigation", new { courseId = Model.CourseId, slideIndex = -1 })

<div class="slide-container">
	<div class="container body-content">
		<div class="row">
			<div class='slide'>
				<h1>Заметки преподавателю <small>@Model.Note.UnitName</small></h1>
				<hr/>
				@Html.Raw(Model.Note.Markdown.RenderMd(Model.Note.File))
			</div>
		</div>
	</div>
</div>
