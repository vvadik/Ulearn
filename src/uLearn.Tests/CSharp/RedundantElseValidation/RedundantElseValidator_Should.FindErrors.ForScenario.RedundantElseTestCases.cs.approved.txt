Строки 11—12: В блоке `if` всегда происходит выход из функции, поэтому `else` можно убрать.
Строки 28—29: В блоке `if` всегда происходит выход из функции, поэтому `else` можно убрать.
Строки 39—40: В блоке `if` всегда происходит выход из функции, поэтому `else` можно убрать.
Строки 50—51: В блоке `if` всегда происходит выход из функции, поэтому `else` можно убрать.
Строки 61—62: В блоке `if` всегда происходит выход из функции, поэтому `else` можно убрать.
Строки 71—74: В блоке `if` всегда происходит выход из функции, поэтому `else` можно убрать.
Строки 82—83: В блоке `if` всегда происходит выход из функции, поэтому `else` можно убрать.